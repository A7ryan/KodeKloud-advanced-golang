package main

// Strings are immutable

func main() {
	// learning := "learning standard library in Go"
	// fun := "library in Go"
	// result := strings.Contains(learning, fun) // must be in sequency
	// fmt.Println(result)

	// tempString := "count the strings count"
	// count := strings.Count(tempString, "count") // is case sensitive
	// fmt.Println(count)

	// learning := "learning is fun in Python"
	// fun := "fun in Python"
	// result := strings.ReplaceAll(learning, fun, "fun in Go") // case sensitive
	// fmt.Println(result)
}
